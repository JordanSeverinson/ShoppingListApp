# Optional: API base URL when NOT using the Vite dev proxy.
# Leave unset for `npm run dev` (requests go through localhost:5173 → localhost:5294).
# Set for production builds, e.g. VITE_API_URL=https://api.example.com
# VITE_API_URL=
